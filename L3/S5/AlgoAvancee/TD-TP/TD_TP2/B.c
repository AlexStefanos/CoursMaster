#include <stdio.h>
#include <stdlib.h>

typedef struct element{
    int nombre;
    Element *suivant;
}element;

typedef struct pile{
    Element *premier;
}pile;

void empiler(Pile *pile, int nvNombre) {
    Element *nouveau = malloc();
    if(pile == NULL || nouveau == NULL)
}

void plusCourtChemin (int **adjacence, int ordre, int s, int *l, int *pred) {
    int *marques, x, y;
    tmpfile *f;

    marques = malloc(sizeof(int) * ordre);
    for(x = 0; x < ordre; x++) {
        marques[x] = 0;
        l[x] = 0;
    }
    marques[s] = 1;
    f = malloc(sizeof(tmpfile) * ordre);

    while() {

    }
}
